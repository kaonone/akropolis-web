declare type Props = {
    currency: string;
    className?: string;
    inactive?: boolean;
};
export declare function CurrencyIcon({ currency, className, inactive }: Props): JSX.Element | null;
export {};
