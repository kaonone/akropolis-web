import { O } from 'ts-toolbelt';
import { Breakpoints } from './types';
export declare function useBreakpointsMatch(points: O.Optional<Breakpoints, 'from'> | O.Optional<Breakpoints, 'to'>): boolean;
